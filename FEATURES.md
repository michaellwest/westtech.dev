# Features

- [x] west-01: Move tags and search to the home page rather than separate links.
- [x] west-02: Use pagination on the home page rather than list everything.
- ~~west-03: Move images to assets folder for optimization at build time.~~ Deferred 2026-05-22 — author favors code snippets over images for new content; migrated images serve fine via Cloudflare CDN.
- west-04: Add copy button to code blocks (top-right of each `pre`, copies inner text, brief "copied" feedback).
- [x] west-05: Shiki theme tuning — language tag on `pre` blocks, review/tune `github-dark` palette, inline-code styling pass.