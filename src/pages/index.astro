---
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import PostCard from "../components/PostCard.astro";

const allPosts = await getCollection("posts", ({ data }) => !data.draft);
const posts = allPosts.sort(
  (a, b) => b.data.date.getTime() - a.data.date.getTime()
);
---

<BaseLayout title="westtech.dev">
  <Header />

  <main class="flex-1 w-full max-w-2xl lg:max-w-4xl mx-auto px-4 sm:px-6 py-10">
    <h1 class="sr-only">westtech.dev — all posts</h1>
    {
      posts.length === 0 ? (
        <p class="text-zinc-500 dark:text-zinc-400">No posts yet. Check back soon.</p>
      ) : (
        posts.map((post) => (
          <PostCard
            title={post.data.title}
            date={post.data.date}
            description={post.data.description}
            tags={post.data.tags}
            slug={post.id}
          />
        ))
      )
    }
  </main>

  <Footer />
</BaseLayout>
